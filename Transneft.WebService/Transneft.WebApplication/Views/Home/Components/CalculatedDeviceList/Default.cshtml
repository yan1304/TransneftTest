@model IEnumerable<CalculatedDevice>

<h2>Расчетные приборы учёта в 2018 году</h2>
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id.ToString()</td>
            </tr>
        }
    </tbody>
</table>